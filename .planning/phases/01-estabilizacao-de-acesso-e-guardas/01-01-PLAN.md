---
phase: 01-estabilizacao-de-acesso-e-guardas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/router/guards/core/guard.ts
  - frontend/pages/auth/login.vue
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
must_haves:
  truths:
    - "Usuario nao autenticado abre /login e permanece no fluxo de autenticacao."
    - "Usuario autenticado que abre /login e enviado para home por permissao, sem loop."
    - "Apos login bem-sucedido, o sistema ignora query redirect e navega para home por perfil."
    - "Rotas protegidas continuam exigindo autenticacao como hoje."
  artifacts:
    - path: "frontend/router/guards/core/guard.ts"
      provides: "Decisao de guarda para rotas unauthenticated e authenticated sem regressao."
    - path: "frontend/pages/auth/login.vue"
      provides: "Destino pos-login padronizado por perfil/permissao."
  key_links:
    - from: "frontend/router/guards/core/guard.ts"
      to: "frontend/router/core/auth.ts"
      via: "meta.authType=unauthenticated na rota /login"
      pattern: "case \"unauthenticated\""
    - from: "frontend/pages/auth/login.vue"
      to: "frontend/stores/predial/auth.ts"
      via: "resultado de login e role para decisao de destino"
      pattern: "authStore.login"
---

<objective>
Estabilizar o fluxo de entrada e pos-login sem refatoracao estrutural.

Purpose: eliminar o comportamento inconsistente de redirecionamento em `/login` mantendo as protecoes atuais.
Output: guard global e login page ajustados com regra unica de destino por permissao.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-estabilizacao-de-acesso-e-guardas/01-CONTEXT.md
@.planning/phases/01-estabilizacao-de-acesso-e-guardas/01-RESEARCH.md
@frontend/router/guards/core/guard.ts
@frontend/pages/auth/login.vue
@frontend/router/core/auth.ts
@frontend/stores/predial/auth.ts

<interfaces>
From `frontend/router/guards/core/guard.ts`:
- `createAuthGuard(): NavigationGuard`
- Branches por `to.meta.authType`: `authenticated | unauthenticated | public`

From `frontend/pages/auth/login.vue`:
- `handleLogin()` realiza `authStore.login(email, password)` e decide navegação final por `predialRole`.

From `frontend/stores/predial/auth.ts`:
- `login(email, password): Promise<{ error?: Error; user?: any }>`
- `role` computado para fallback de permissao.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ajustar guarda da rota /login para sessao hidratada e redirecionamento por permissao</name>
  <read_first>
    - frontend/router/guards/core/guard.ts
    - frontend/router/core/auth.ts
    - .planning/phases/01-estabilizacao-de-acesso-e-guardas/01-CONTEXT.md
  </read_first>
  <files>frontend/router/guards/core/guard.ts</files>
  <behavior>
    - Teste 1: quando `authType="unauthenticated"` e sessao ausente, navegação continua para `/login` (AUTH-01, D-01).
    - Teste 2: quando `authType="unauthenticated"` e sessao presente, usuário vai para home por permissao, sem usar query redirect (AUTH-02, D-03).
    - Teste 3: branch de `authType="authenticated"` continua exigindo sessão e preserva redirect para login quando ausente (AUTH-03, D-05).
  </behavior>
  <action>
    Aplicar correção pontual no branch `unauthenticated` do guard (D-02 e D-05):
    1) manter `await authClient.getSession()` antes da decisão final (AUTH-04);
    2) remover uso de `getSafeRedirect(to.query.redirect, ...)` para usuário autenticado em rota de auth;
    3) redirecionar autenticado em `/login` para destino padrão por permissao (D-03), reutilizando o mesmo mapeamento de role usado no login page (ou helper local mínimo, sem refatoração estrutural).
    Não alterar semântica do branch `authenticated`.
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <done>Guard encaminha /login de forma previsível para autenticado/não autenticado e mantém regras existentes de rota protegida.</done>
  <acceptance_criteria>
    - AUTH-01 atendido sem fallback para landing.
    - AUTH-02 atendido com destino autenticado por permissão.
    - AUTH-03 preservado no branch authenticated.
    - AUTH-04 garantido pelo await de sessão antes de decidir.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Padronizar pós-login para ignorar redirect e navegar por perfil</name>
  <read_first>
    - frontend/pages/auth/login.vue
    - frontend/stores/predial/auth.ts
    - .planning/phases/01-estabilizacao-de-acesso-e-guardas/01-CONTEXT.md
  </read_first>
  <files>frontend/pages/auth/login.vue</files>
  <behavior>
    - Teste 1: login bem-sucedido sempre navega para home/dash por `predialRole` (D-04).
    - Teste 2: querystring `?redirect=...` não altera destino após autenticação (AUTH-02, D-04).
    - Teste 3: papel não mapeado mantém fallback atual sem quebrar fluxo.
  </behavior>
  <action>
    Consolidar a decisão de navegação pós-login em regra única por perfil/permissão (D-04), ignorando completamente parâmetro `redirect`.
    Manter mudanças localizadas em `login.vue` (D-05), sem alterar contrato do store.
  </action>
  <verify>
    <automated>bun run typecheck && bun run lint:fix && bun lint</automated>
  </verify>
  <done>Pós-login não depende de redirect e segue destino padrão por perfil em todos os casos de sucesso.</done>
  <acceptance_criteria>
    - AUTH-02 atendido sem loop.
    - AUTH-04 compatível com sessão já estabilizada pelo guard.
    - Fluxo visual e mensagens de erro permanecem funcionais.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Executar gate de qualidade obrigatório da fase</name>
  <read_first>
    - .cursor/rules/quality-assurance.mdc
    - .planning/phases/01-estabilizacao-de-acesso-e-guardas/01-VALIDATION.md
  </read_first>
  <files>frontend/router/guards/core/guard.ts, frontend/pages/auth/login.vue</files>
  <action>
    Rodar checks obrigatórios do workspace nesta ordem:
    `bun run typecheck` -> `bun run lint:fix` -> `bun lint` -> `bun prettier`.
    Corrigir eventuais erros introduzidos pelas mudanças sem expandir escopo.
  </action>
  <verify>
    <automated>bun run typecheck && bun run lint:fix && bun lint && bun prettier</automated>
  </verify>
  <done>Suite obrigatória de qualidade finaliza com sucesso e sem erros pendentes.</done>
  <acceptance_criteria>
    - Sem erros de TypeScript.
    - Sem erros de lint.
    - Sem pendências de formatação.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Validar localmente (D-06) os cenários: `/login` anônimo, `/login` autenticado e login com query redirect.
</verification>

<success_criteria>
As quatro verdades de AUTH-01..AUTH-04 são observáveis em ambiente local sem regressão de rota protegida.
</success_criteria>

<output>
After completion, create `.planning/phases/01-estabilizacao-de-acesso-e-guardas/01-01-SUMMARY.md`
</output>
